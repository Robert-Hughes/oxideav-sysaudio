[package]
name = "oxideav-sysaudio"
version = "0.1.1"
edition = "2021"
rust-version = "1.80"
license = "MIT"
repository = "https://github.com/OxideAV/oxideav-sysaudio"
homepage = "https://github.com/OxideAV/oxideav-sysaudio"
authors = ["Mark Karpeles"]
description = "Pure-Rust audio output: runtime-loaded native backends (ALSA, PulseAudio, WASAPI, CoreAudio, …) with no C build-time linkage"
readme = "README.md"
keywords = ["audio", "output", "alsa", "pulseaudio", "wasapi"]
categories = ["multimedia::audio"]

[features]
# Every backend compiles into every build by default. At runtime, `probe()`
# skips the ones whose shared library fails to load or whose dummy-open
# fails, so having more features on than the OS provides is harmless.
default = ["alsa", "pulse", "pipewire", "oss", "wasapi", "asio", "coreaudio", "aaudio"]
alsa = []
pulse = []
pipewire = []
oss = []
wasapi = []
asio = []
coreaudio = []
aaudio = []
# Virtual hardware-free backend for tests/CI: adds a `"mock"` driver
# (always last in the preference order) that renders the callback into
# a discard/capture sink from a paced worker thread. Deliberately NOT
# in the default set — with it enabled, `probe()` never comes back
# empty, which changes `open_default()`'s failure behaviour.
mock = []

[dependencies]
libloading = "0.9"
